{
  "name": "void-task-manager",
  "version": "1.0.0",
  "description": "VOID — Space Command Center Task Manager",
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cross-env NODE_ENV=production npm run build && cd server && cross-env NODE_ENV=production npm start",
    "start:prod": "npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^10.1.0"
  }
}
